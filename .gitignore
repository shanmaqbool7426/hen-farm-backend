# dependencies
node_modules

# compiled output
dist
*.tsbuildinfo
# api/_lib is committed on purpose (see api/handler.js) - Vercel's build
# command has repeatedly failed to actually run for this project, so the
# working bundle needs to already exist in the repo, not be generated fresh
# on every deploy.

# secrets - real DB credentials live here, never commit
.env
.env.local
.env.*.local
