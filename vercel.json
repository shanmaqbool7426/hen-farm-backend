{
  "buildCommand": "echo 'no build needed - api/handler.js and api/_lib are a pre-built, committed bundle'",
  "functions": {
    "api/handler.js": {
      "maxDuration": 30
    }
  },
  "crons": [
    {
      "path": "/api/cron/daily-eggs",
      "schedule": "0 0 * * *"
    }
  ],
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/handler"
    },
    {
      "source": "/",
      "destination": "/api/handler"
    }
  ]
}
